name: Sredictio Deployment Test

on:
  pull_request:
    branches: 
      - master
  push:
    branches: 
      - master

jobs:
  Build-for-Ubuntu:
    name: Run Sredictio On Ubuntu
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v1
    - name: Set up Python 3.7
      uses: actions/setup-python@v1
      with:
        python-version: 3.7
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
    - name: Download NLTK VADER Lexicon
      run: |
        python -c "import nltk; nltk.download('vader_lexicon')"
    - name: Run Sredictio
      run: |
        python Main.py "Stock History.csv" Model_LBW-20_NOI-100000.zip "Boeing" BA

  Build-for-macOS:
    name: Run Sredictio On MacOS
    runs-on: macOS-latest

    steps:
    - uses: actions/checkout@v1
    - name: Set up Python 3.7
      uses: actions/setup-python@v1
      with:
        python-version: 3.7
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
    - name: Download NLTK VADER Lexicon
      run: |
        python -c "import nltk; nltk.download('vader_lexicon')"
    - name: Run Sredictio
      run: |
        python Main.py "Stock History.csv" Model_LBW-20_NOI-100000.zip "Boeing" BA

  Build-for-Windows:
    name: Run Sredictio On Windows
    runs-on: windows-latest

    steps:
    - uses: actions/checkout@v1
    - name: Set up Python 3.7
      uses: actions/setup-python@v1
      with:
        python-version: 3.7
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
    - name: Download NLTK VADER Lexicon
      run: |
        python -c "import nltk; nltk.download('vader_lexicon')"
    - name: Run Sredictio
      run: |
        python Main.py "Stock History.csv" Model_LBW-20_NOI-100000.zip "Boeing" BA

